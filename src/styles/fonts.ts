/**
 * Centralized font definitions for the entire application.
 * All components should import from here instead of declaring local font constants.
 */
export const FONT_SANS = "'Google Sans Flex', system-ui, -apple-system, sans-serif";
